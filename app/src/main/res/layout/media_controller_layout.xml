<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#aaa">

    <ImageView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:contentDescription="@null"
            android:id="@+id/sheet_music_thumbnail"
            android:src="@drawable/am"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/sheet_play_pause_button"/>

    <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:id="@+id/sheet_play_pause_button"
            android:layout_marginBottom="32dp"
            android:contentDescription="@null"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:src="@drawable/ic_play_arrow_white_24dp"/>

    <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_skip_next_white_24dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/sheet_play_pause_button"
            app:layout_constraintBottom_toBottomOf="@+id/sheet_play_pause_button"
            app:layout_constraintStart_toEndOf="@+id/sheet_play_pause_button"
            android:contentDescription="@null"
            android:id="@+id/sheet_next_button"/>

    <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_skip_previous_white_24dp"
            app:layout_constraintTop_toTopOf="@+id/sheet_play_pause_button"
            app:layout_constraintBottom_toBottomOf="@+id/sheet_play_pause_button"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/sheet_play_pause_button"
            android:contentDescription="@null"
            android:id="@+id/sheet_prev_button"/>

</androidx.constraintlayout.widget.ConstraintLayout>